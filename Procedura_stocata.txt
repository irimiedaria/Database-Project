PROCEDURE `Afisare_6b`; CREATE DEFINER=`root`@`localhost` PROCEDURE `Afisare_6b`() NOT DETERMINISTIC CONTAINS SQL SQL SECURITY DEFINER BEGIN SELECT zona, moneda, SUM(pret) as sum FROM Spatiu join Oferta using(id_spatiu) GROUP BY zona, moneda WITH ROLLUP; END
